<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx/8.0" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="tn.esprit.Controllers.ListUsersController"
            style="-fx-background-color: #f4f4f4; -fx-padding: 20px;"
            stylesheets="@CSS/stylesListUser.css">

    <!-- Header avec titre et barre de recherche -->
    <top>
        <VBox spacing="10">
            <HBox alignment="CENTER_RIGHT" spacing="10" style="-fx-padding: 10px;">
                <Label text="📋 Liste des Employés" style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #0047AB;"/>
                <Pane HBox.hgrow="ALWAYS"/>
                <Button text="+ Ajouter Employé" onAction="#showAddEmployeePopup"
                        styleClass="gradient-button"/>
            </HBox>

            <!-- Barre de recherche -->
            <HBox alignment="CENTER_LEFT" spacing="5" styleClass="searchBox">
                <TextField fx:id="searchField" promptText="Rechercher un employé..."
                           styleClass="searchInput" maxWidth="250"/>

                <Button fx:id="searchButton" onAction="#handleSearch"
                        styleClass="searchButton" alignment="TOP_RIGHT" >
                    <graphic>
                        <ImageView fitWidth="20" fitHeight="20" >
                            <image>
                                <Image url="@images/search-1.jpg"/>
                            </image>
                        </ImageView>
                    </graphic>
                </Button>
            </HBox>
        </VBox>
    </top>

    <!-- TableView avec de l'espacement -->
    <center>
        <VBox spacing="10" style="-fx-padding: 20px;">
            <TableView fx:id="employeeTable" style="-fx-background-color: white;">
                <columns>
                    <TableColumn fx:id="colProfile" text="Profile" prefWidth="100"/>
                    <TableColumn fx:id="colFirstName" text="Nom" prefWidth="150"/>
                    <TableColumn fx:id="colLastName" text="Prénom" prefWidth="150"/>
                    <TableColumn fx:id="colEmail" text="Email" prefWidth="200"/>
                    <TableColumn fx:id="colBirthday" text="Date de Naissance" prefWidth="120"/>
                    <TableColumn fx:id="colJoiningDate" text="Date d'embauche" prefWidth="120"/>
                    <TableColumn fx:id="colRole" text="Rôle" prefWidth="150"/>
                    <TableColumn fx:id="colNumTel" text="Téléphone" prefWidth="150"/>
                    <TableColumn fx:id="colCongeRestant" text="Congés Restants" prefWidth="120"/>
                    <TableColumn fx:id="colTtRestants" text="TT Restants" prefWidth="120"/>
                </columns>
            </TableView>
        </VBox>
    </center>

</BorderPane>
